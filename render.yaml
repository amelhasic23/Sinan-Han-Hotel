services:
  - type: web
    name: sinan-han-hotel
    env: node
    rootDir: hotel-booking-system
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
